Sources for the plugin `plugin.gamesparks`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.gamesparks" = {
            publisherId = "com.gamesparks",
        },
    },
}
```
